# PID-line-follower--Arduino-UNO
Code for a two-wheeled robot that autonomously follows a black line on a white surface. A 5-channel IR sensor array detects the line position, a PID algorithm computes a steering correction, and a L298N motor driver applies differential speed to two DC gear motors to keep the robot centred.
